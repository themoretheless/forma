# Production readiness — рабочий аудит

Цель: подготовить Forma и Studio к надёжной эксплуатации и воспроизводимому
выпуску. Статус: **не завершено**. Зелёные unit-тесты не доказывают готовность
всего продукта. Функциональные ограничения языка/runtime описаны в LANGUAGE,
BINDINGS и карте покрытия; их совместимость с выпуском ещё требует аудита.

## Проверяемые требования

- [ ] Чистая установка из lockfiles и воспроизводимая сборка текущего WASM.
- [ ] JS + Rust проверки, native hosts и binding example; объяснены ignored tests.
- [ ] Production web bundle: запуск, редактирование, импорт/экспорт, сохранение,
      восстановление, сценарии, дерево, инспектор, контекстное меню, клавиатура.
- [ ] Ошибки хранилища, повреждённые проекты и ошибки WASM не ломают весь UI
      и не приводят к ложному сообщению о сохранении.
- [ ] Недоверенные проекты и bridge/runner-запросы: валидация путей, лимиты,
      изоляция локального исполнения, отсутствие непреднамеренного запуска.
- [ ] Повторный mount/dispose, длительная работа и большие документы:
      ограниченные кэши/буферы, отсутствие накопления подписок и runtime.
- [ ] Упаковка проверенной пары Studio/WASM, проверка архивов и инструкции
      эксплуатации/обновления/восстановления. CI покрывает релизные gates.
- [ ] Проверен полный итоговый diff; не осталось необъяснённых блокеров.

## 13 сентября: исходная проверка и первый фикс

Рабочее дерево было чистым, HEAD faa750f. Version check прошёл.
Production build прошёл. Rust `cargo test --locked --offline --manifest-path
vector-ui/Cargo.toml --features gpu --lib --tests`: 127 passed, 2 ignored.
Ignored hardware checks ещё предстоит отдельно оценить/выполнить.

Исправлены незащищённые обращения к localStorage/sessionStorage: доступ может
бросить SecurityError, а запись — QuotaExceededError. Настройки больше не
ломают старт и переключение режима. Сохранение проекта при отказе записи
продолжает явно показывать «Не сохранено: экспортируйте проект».

Добавлена единая проверка структуры проекта/путей для восстановления,
импорта, создания файла и bridge-команд. Повреждённая запись не удаляется
при восстановлении: стартует исходный проект. Тесты проверяют отказ доступа,
квоту, неверную структуру и опасные сегменты пути. Браузерные сценарии ошибок
и удобное извлечение повреждённой записи пока не проверены/не завершены.

## Приоритет GPU / Metal (уточнение цели)

Цель расширена: оптимизировать весь продукт, особенно GPU и Metal.
Аппаратные проверки внутри песочницы завершились ошибкой отсутствия Metal
adapter. Повтор вне песочницы прошёл: два GPU unit-теста и один integration
тест CPU/GPU-согласованности при дробном DPI. Это не дефект renderer.

Первичный release gpu_bench вне песочницы:
- 800×400: CPU submit 0,460 мс; submit + GPU wait 3,892 мс.
- 3840×2160: CPU submit 0,564 мс; submit + GPU wait 8,320 мс.
- Geometry reuploads после прогрева: 0 в обоих сценариях.

Это одиночный базовый запуск, не доказательство ускорения и не FPS окна.
Следующая проверка: большие multi-control сцены, доля изменённых paint slots,
объём queue.write_buffer при локальной анимации и GPU timestamp distribution.
Сейчас изменение одного paint slot передаёт весь paint buffer. Перед изменением
этого пути нужны парные замеры и проверки корректности/лимитов write ranges.

## Частичное обновление paint buffer

В native wgpu renderer вместо полной передачи массива цветов пишется один
непрерывный диапазон от первого до последнего изменённого f32. Выравнивание
4 байта; при изменении длины/перевыделении буфера передаётся весь payload.
Определение диапазона не создаёт коллекций; сравниваются биты (включая -0/NaN).
Unit-тест проверяет восстановление, resize, разделённые изменения и особые биты.

Metal integration test с чтением пикселей: локальный hover отправляет 12 вместо
64 байт при DPI 1 / 1,25 / 2. Соседний контрол побайтно неизменен, повторных
загрузок геометрии нет. Большие сцены и парные замеры времени ещё необходимы;
ускорение FPS/времени кадра на основании этих байтов не заявляется.

## WebGPU paint transport

Браузерный storage pool поддерживает частичную запись paint payload с
выравниванием на 4 байта. Неизменившиеся байты не отправляются. При смене сцены,
изменении длины или после неудачной записи выполняется полная запись.
CPU snapshot копируется в переиспользуемый Uint8Array: одна дополнительная
копия paint payload на активный painter, освобождаемая вместе с pool.
Geometry/tiles не получают дополнительные CPU snapshots.
14 целевых JS-тестов прошли, включая восстановление после частичного отказа.
Это проверки API с mock GPU device; аппаратный браузерный WebGPU-тест и
измерения на больших сценах пока остаются открытыми.

## Сборка и происхождение артефактов

macOS native `forma` check прошёл; native binding example — 8 тестов;
WebView-хост с `.github/fixtures/actions.rs` также компилируется.
Найден старый build-info (be0f74a вместо текущего faa750f): WASM пересобран.

Упаковка теперь проверяет SHA-256 исходных runtime-входов, библиотеки контролов
и build-скрипта, а также четырёх JS/WASM/TypeScript артефактов. Сборка проверяет,
что входы не изменились за время выполнения. Это закрывает случай устаревшего
WASM при незакоммиченных правках внутри одного SHA коммита.

Production build и package:release прошли, контрольные суммы и наличие
build-info в двух текущих архивах проверены. Архивы пока локальные, dirty=true,
не опубликованы и не считаются выпуском из чистого checkout. Остались чистая
установка/CI на всех ОС и функциональные проверки production bundle.
