# Production readiness — рабочий аудит

Цель: подготовить Forma и Studio к надёжной эксплуатации и воспроизводимому
выпуску. Статус: **не завершено**. Зелёные unit-тесты не доказывают готовность
всего продукта. Функциональные ограничения языка/runtime описаны в LANGUAGE,
BINDINGS и карте покрытия; их совместимость с выпуском ещё требует аудита.

## Проверяемые требования

- [ ] Чистая установка из lockfiles и воспроизводимая сборка текущего WASM.
- [ ] JS + Rust проверки, native hosts и binding example; объяснены ignored tests.
- [ ] Production web bundle: запуск, редактирование, импорт/экспорт, сохранение,
      восстановление, сценарии, дерево, инспектор, контекстное меню, клавиатура.
- [ ] Ошибки хранилища, повреждённые проекты и ошибки WASM не ломают весь UI
      и не приводят к ложному сообщению о сохранении.
- [ ] Недоверенные проекты и bridge/runner-запросы: валидация путей, лимиты,
      изоляция локального исполнения, отсутствие непреднамеренного запуска.
- [ ] Повторный mount/dispose, длительная работа и большие документы:
      ограниченные кэши/буферы, отсутствие накопления подписок и runtime.
- [ ] Упаковка проверенной пары Studio/WASM, проверка архивов и инструкции
      эксплуатации/обновления/восстановления. CI покрывает релизные gates.
- [ ] Проверен полный итоговый diff; не осталось необъяснённых блокеров.

## 13 сентября: исходная проверка и первый фикс

Рабочее дерево было чистым, HEAD faa750f. Version check прошёл.
Production build прошёл. Rust `cargo test --locked --offline --manifest-path
vector-ui/Cargo.toml --features gpu --lib --tests`: 127 passed, 2 ignored.
Ignored hardware checks ещё предстоит отдельно оценить/выполнить.

Исправлены незащищённые обращения к localStorage/sessionStorage: доступ может
бросить SecurityError, а запись — QuotaExceededError. Настройки больше не
ломают старт и переключение режима. Сохранение проекта при отказе записи
продолжает явно показывать «Не сохранено: экспортируйте проект».

Добавлена единая проверка структуры проекта/путей для восстановления,
импорта, создания файла и bridge-команд. Повреждённая запись не удаляется
при восстановлении: стартует исходный проект. Тесты проверяют отказ доступа,
квоту, неверную структуру и опасные сегменты пути. Браузерные сценарии ошибок
и удобное извлечение повреждённой записи пока не проверены/не завершены.

## Приоритет GPU / Metal (уточнение цели)

Цель расширена: оптимизировать весь продукт, особенно GPU и Metal.
Аппаратные проверки внутри песочницы завершились ошибкой отсутствия Metal
adapter. Повтор вне песочницы прошёл: два GPU unit-теста и один integration
тест CPU/GPU-согласованности при дробном DPI. Это не дефект renderer.

Первичный release gpu_bench вне песочницы:
- 800×400: CPU submit 0,460 мс; submit + GPU wait 3,892 мс.
- 3840×2160: CPU submit 0,564 мс; submit + GPU wait 8,320 мс.
- Geometry reuploads после прогрева: 0 в обоих сценариях.

Это одиночный базовый запуск, не доказательство ускорения и не FPS окна.
Следующая проверка: большие multi-control сцены, доля изменённых paint slots,
объём queue.write_buffer при локальной анимации и GPU timestamp distribution.
Сейчас изменение одного paint slot передаёт весь paint buffer. Перед изменением
этого пути нужны парные замеры и проверки корректности/лимитов write ranges.

## Частичное обновление paint buffer

В native wgpu renderer вместо полной передачи массива цветов пишется один
непрерывный диапазон от первого до последнего изменённого f32. Выравнивание
4 байта; при изменении длины/перевыделении буфера передаётся весь payload.
Определение диапазона не создаёт коллекций; сравниваются биты (включая -0/NaN).
Unit-тест проверяет восстановление, resize, разделённые изменения и особые биты.

Metal integration test с чтением пикселей: локальный hover отправляет 12 вместо
64 байт при DPI 1 / 1,25 / 2. Соседний контрол побайтно неизменен, повторных
загрузок геометрии нет. Большие сцены и парные замеры времени ещё необходимы;
ускорение FPS/времени кадра на основании этих байтов не заявляется.

## WebGPU paint transport

Браузерный storage pool поддерживает частичную запись paint payload с
выравниванием на 4 байта. Неизменившиеся байты не отправляются. При смене сцены,
изменении длины или после неудачной записи выполняется полная запись.
CPU snapshot копируется в переиспользуемый Uint8Array: одна дополнительная
копия paint payload на активный painter, освобождаемая вместе с pool.
Geometry/tiles не получают дополнительные CPU snapshots.
14 целевых JS-тестов прошли, включая восстановление после частичного отказа.
Это проверки API с mock GPU device; аппаратный браузерный WebGPU-тест и
измерения на больших сценах пока остаются открытыми.

## Сборка и происхождение артефактов

macOS native `forma` check прошёл; native binding example — 8 тестов;
WebView-хост с `.github/fixtures/actions.rs` также компилируется.
Найден старый build-info (be0f74a вместо текущего faa750f): WASM пересобран.

Упаковка теперь проверяет SHA-256 исходных runtime-входов, библиотеки контролов
и build-скрипта, а также четырёх JS/WASM/TypeScript артефактов. Сборка проверяет,
что входы не изменились за время выполнения. Это закрывает случай устаревшего
WASM при незакоммиченных правках внутри одного SHA коммита.

Production build и package:release прошли, контрольные суммы и наличие
build-info в двух текущих архивах проверены. Архивы пока локальные, dirty=true,
не опубликованы и не считаются выпуском из чистого checkout. Остались чистая
установка/CI на всех ОС и функциональные проверки production bundle.

## Статический browser smoke после c22339e

Проверено через обычный Python HTTP server на 127.0.0.1:4174, без Vite
middleware. Исходный HTML-проект открывается; при выборе векторного renderer
проект без components/Button.ui показывает понятную диагностику и сохраняет
последний preview. Проект studio-controls загружает WASM и после асинхронной
инициализации показывает `GPU · vector` (WebGPU), диагностика 0.
Выбор PrimaryButton в дереве открывает инспектор. Ввод width=132 клавиатурой
с уходом фокуса обновляет исходник; после reload инспектор показывает 132.
В browser console за этот сценарий нет warning/error. Программный fill без
предварительного фокуса не воспроизвёл пользовательский change; для проверки
использовались click, выделение, посимвольный ввод и Tab.

Это smoke запуска, WebGPU, выбора, изменения и восстановления в отдельном
origin. Он не доказывает отсутствие GPU pixel regressions, утечек при долгой
работе, исправность импорта/экспорта или всех клавиатурных сценариев. Последние
пункты остаются открытыми. Показанный сначала CPU — промежуточное состояние
до завершения асинхронного выбора GPU, не окончательный fallback.

## Metal multi-control benchmark

Добавлен `vector-ui/examples/gpu_scene_bench.rs`. Команда:
`cargo run --locked --offline --release --manifest-path vector-ui/Cargo.toml --features gpu --example gpu_scene_bench`.
Apple M5 / Metal, target 1000×2560, 20 кадров прогрева и 200 измерений на сцену.
Плоские кнопки 1000×10; hover первой чередуется с выходом указателя.
Весь набор видим в target. Timestamp query измеряет render pass, CPU submit
не включает pointer/update, ожидание GPU, чтение timestamps или presentation.

| Контролы | Paint bytes/frame (полный payload) | CPU submit p50/p95, ms | GPU p50/p95, ms |
|---|---|---|---|
| 100 | 12 (3200) | 0.486 / 3.501 | 1.365 / 2.717 |
| 256 | 12 (8192) | 0.484 / 1.037 | 2.059 / 3.524 |

Обе сцены: 0 новых GPU-буферов и 0 повторных загрузок геометрии после прогрева;
200 валидных GPU timestamp samples. Это одиночный запуск с profiling overhead,
не парное доказательство ускорения. Проверки объёма передач встроены в пример.
Runtime::from_sources ограничивает сцену 256 контролами: запрос 1000 отклонён
валидатором; этот масштаб остаётся открытым для другого API/отдельного аудита.
Первые попытки сцены с Row и высотой 1 не были валидными измерениями; в таблице
только завершённый запуск плоской сцены с работающей hit area высотой 10.

## WebGPU storage allocation limits

Проверка пула показала намеренное удержание максимальной ёмкости до destroy,
а не уменьшение при каждом небольшом payload. Native проверяет минимум
max_storage_buffer_binding_size и max_buffer_size; WebGPU теперь также
учитывает оба лимита перед ростом буфера. Regression test задаёт меньший
maxBufferSize: допустимая аллокация ограничивается им, недопустимый payload
не меняет пул/счётчики и не вызывает write; destroy освобождает буфер.
15 целевых GPU JS-тестов и production build прошли. Это доказательство
соблюдения лимита и API lifecycle в mock, не измерение RSS/driver allocations
и не завершение длительной проверки памяти.

## Защита повреждённого проекта от автосохранения

writeProject перед заменой невалидного JSON сохраняет исходные байты в
`:recovery`. Квота/отказ доступа или занятый другой записью recovery-слот
блокируют замену оригинала. UI сообщает о повреждении при старте и позволяет
скачать исходную запись без её разбора; ранее сохранённая recovery-копия
также доступна после reload. Три storage-теста прошли, включая конфликт
recovery и отказ записи. Production build прошёл. Браузерная проверка
повреждённой записи и скачивания ещё необходима.

## Повторное сохранение без изменений

writeProject теперь пропускает JSON.parse старой записи и setItem, если она
побайтно совпадает с уже валидированным и сериализованным проектом. Это не
добавляет долгоживущий кэш строк. Проверка повреждений остаётся для отличающихся
данных. Regression test требует успешного no-op даже при бросающем setItem.
Полный JS suite до этого последнего fast path: 256 passed; затем все 4 storage
теста и production build прошли. Исходный замер mock-хранилища (без диска):
повторная сериализация/валидация/parse 10 МБ — p50 41.88 мс, p95 43.45 мс.
Сериализация остаётся синхронной; влияние на задержку ввода больших проектов
ещё требует браузерного профиля. Ускорение всего приложения не заявляется.

## Очистка временного design runner

Вычисление design.* раньше оставляло каждый каталог forma-design-* с Rust
исходниками, бинарником и результатами. Теперь finally удаляет созданный
каталог после чтения результатов, ошибки компилятора или ошибки исполнения.
Integration test с настоящим rustc проверяет результат 42 и отсутствие
каталогов runner после всех трёх исходов. Кэш xcrun внутри тестового TMPDIR
не считается ресурсом runner. Тест прошёл на macOS; межплатформенная CI
проверка остаётся необходимой. Исполнение Rust по-прежнему не является
песочницей; очистка временных файлов не обеспечивает изоляцию кода.

## Ограничение загрузки результатов design.*

Результаты ранее загружались через Promise.all(readFile) без ограничения размера.
Теперь readDesignValues читает последовательно в один scratch 1 МиБ + 1 байт,
проверяя суммарный объём до превращения очередного результата в строку.
Общий лимит — 1 МиБ UTF-8; при превышении возвращается ошибка, finally runner
удаляет временный каталог. Чтение не полагается на предварительный stat.size:
рост файла также ограничен числом прочитанных байт. Тесты проверяют ровно лимит,
превышение одним файлом и превышение суммой файлов; тест с rustc также прошёл.
Это ограничивает память Node при чтении результатов, но не память/диск самого
исполняемого Rust-кода. Изоляция исполнения остаётся отдельным открытым пунктом.

## Сводная проверка текущего этапа

Общий JS suite: 259 passed, 0 failed (38.9 s); production build прошёл.
После правки имени Windows executable повторно прошли оба design runner теста.
CI runtime matrix теперь запускает их на Linux/macOS/Windows; удалённый CI
ещё не выполнен, локальный результат относится к macOS. Изменения этого этапа
проверены diff --check; весь production-аудит остаётся незавершённым.

## Отказ инициализации WebGPU uniform buffer

Создание uniform buffer перенесено внутрь try инициализации painter. При его
отказе canvas unconfigure и device.destroy теперь выполняются до возврата
ошибки. Это закрывает путь, где context уже был настроен, но вызывающий код
ещё не получил painter для destroy. Новый regression test инжектирует отказ
createBuffer и проверяет оба освобождения. 16 GPU JS tests и build прошли.
Аппаратный OOM этим mock-тестом не симулируется.

## Повторная проверка GPU lifecycle и Metal

18 GPU JS-тестов прошли: 1000 неизменённых draw сохраняют объём буферов,
число аллокаций и переданных байт; destroy освобождает buffers и повторно
ничего не делает. device.destroy теперь выполняется через finally даже при
ошибке context.unconfigure. Это учёт ресурсов mock API, не heap/RSS профиль.
Production build прошёл. Повторный реальный Metal запуск всех ignored tests:
2 library + 1 integration passed (ресурсы profiling renderer, CPU/GPU Reveal
при разных DPI и независимость пикселей анимируемых контролов).

## Чистая установка frontend из lockfile

Node 22.23.2 / npm 10.9.8, отдельный /private/tmp/forma-clean-install.pghKBF.
`npm ci --offline --no-audit --no-fund` установил 124 пакета в пустой node_modules.
В каталог скопированы текущие tracked исходники и существующий WASM artifact;
`npm run build` и `npm test`: 262 passed, 0 failed, 38.9 s.
SHA-256 всех 114 файлов dist полностью совпадают с рабочей сборкой.
Это подтверждает чистую установку JS-зависимостей и воспроизводимость frontend
на этой машине с тем же WASM. WASM не пересобирался в этом опыте, npm cache
был существующим, удалённый registry и сборка на других ОС не проверялись.

## Пересборка WASM в отдельном Cargo target

Исправлен build:wasm: Cargo и wasm-bindgen теперь используют один targetDir;
раньше CARGO_TARGET_DIR мог направить Cargo в новый каталог, но wasm-bindgen
читал старый vector-ui/target. Проверен запуск с
CARGO_TARGET_DIR=/private/tmp/forma-clean-wasm-target и CARGO_NET_OFFLINE=true.
Зависимости и runtime скомпилированы за 13.69 s. Все четыре generated JS/WASM/d.ts
артефакта побайтно совпадают с предыдущими; SHA-256 WASM:
47166527b7af5316821d3b1c71b9faf2742ffbd63ea9ebc2a10ddc20e4fedb01.
Это пересборка без прежних target artifacts, но с установленным toolchain и
существующим Cargo registry cache. Не является проверкой чистого Git release
или нового окружения другой ОС.
На пересобранном WASM: 262 JS tests passed (34.24 s), production build и
package:release прошли. Пакеты локальные, dirty=true; опубликованным выпуском
не считаются.

## Проверка библиотеки контролов при упаковке

package:release теперь сравнивает digest всей vector-ui/controls с копиями
public/vector-pkg/controls и dist/vector-pkg/controls. Ранее сверялись только
четыре JS/WASM/d.ts файла, что пропускало устаревшие или лишние контролы.
Проверка отказа: отдельный новый probe-файл последовательно добавлен в каждую
копию; обе попытки упаковки отклонены именно с диагностикой controls. Probe
удалён в finally; последующая обычная упаковка прошла. Исходники не менялись.
Эта проверка не доказывает актуальность всего frontend bundle относительно src.

## Чистота checkout на момент упаковки

CI packaging теперь проверяет свежий git status, а не только поле dirty
в build-info. Проверка отказа: обе generated metadata временно выставлены
в dirty=false, при текущих tracked правках CI упаковка всё равно отклонена
с диагностикой чистоты checkout. Оригинальные metadata восстановлены в finally.
Обычная локальная упаковка с dirty=true прошла до этой диагностической пробы.
Это закрывает CI-случай изменений после сборки; fingerprint всего frontend
bundle при локальной упаковке по-прежнему остаётся открытым.

## Неверные payload локальных runners

WebSocket handlers design/form/rust теперь безопасно читают поля null/undefined.
Native runner разбирает request внутри try, Rust runner проверяет контейнер
files до подготовки проекта. Неверный запрос возвращает error и освобождает
busy, следующий запрос не остаётся заблокированным. Новый тест проверяет
null, undefined, массив, пустой объект и число для обоих runners; совместно
с vector runner 10 tests passed, production build прошёл. Это проверка
ошибочных входов, не доказательство полной изоляции локального исполнения.

## Срок жизни снимка design request

Проверен путь src/main.js: design-data отправляется только обработчиком кнопки
после confirm; импорт сам этот путь не вызывает. После совпавшего ответа
designRequest теперь очищается до обработки error/устаревшего результата.
Ранее JSON-копия всего проекта оставалась захваченной обработчиком до нового
запроса/закрытия страницы. Локальная request-ссылка живёт только до возврата
обработчика; дубликаты и посторонние ответы игнорируются. Build и diff --check
прошли. Отсутствие ответа/разрыв WebSocket ещё требуют отдельной обработки.

## Завершение ожидания design response

Добавлены 45-секундный timeout, отмена ожидания при vite:ws:disconnect и
очистка таймера/снимка при dispose. Кнопка снова доступна; поздний ответ
не применяется и автоматического повторного исполнения нет. Сообщение явно
указывает неизвестный исход. Тест выполняет реальный блок обработчика из
main.js в VM с fake transport/clock: timeout, disconnect, late reply и
следующий успешный запрос; прошёл. Production build прошёл. Это проверка
логики обработчика, реальный сетевой разрыв браузера ещё не воспроизведён.

## Очистка design HMR subscriptions

Обработчик forma:design-data-result теперь именован и явно снимается вместе
с vite:ws:disconnect при dispose. Тест дополнен завершением модуля во время
ожидания: после dispose не остаётся ни handler, ни timer, кнопка разблокирована.
Тест и production build прошли. Проверяется реальный блок main.js в VM;
фактические retained heap объекты браузера этим тестом не измеряются.

## Интегрированная проверка runners/build/lifecycle

Полный JS suite: 264 passed, 0 failed, 33.17 s. Дополнительно удаляется
forma:rust-output listener и очищается nativeBuffer при dispose; production
build после этой правки прошёл. Последние изменения объединяют обработку
ошибочных runner inputs, design request timeout/dispose, GPU cleanup и
проверки сборки/упаковки. Весь readiness checklist ещё не закрыт.
